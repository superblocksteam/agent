{{- if .Values.externalSecrets.enabled }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: dataplane-superblocks-key
  annotations:
    external-secrets.io/force-sync: {{ now | date "2006-01-02T15:04:05Z" | quote }}
spec:
  refreshInterval: {{ .Values.externalSecrets.refreshInterval | default "1h" }}
  secretStoreRef:
    name: {{ .Values.externalSecrets.secretStoreRef.name }}
    kind: {{ .Values.externalSecrets.secretStoreRef.kind | default "ClusterSecretStore" }}
  target:
    name: dataplane-superblocks-key
    creationPolicy: Owner
  data:
    {{- toYaml .Values.externalSecrets.superblocksKey.data | nindent 4 }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: dataplane-secrets-encryption
  annotations:
    external-secrets.io/force-sync: {{ now | date "2006-01-02T15:04:05Z" | quote }}
spec:
  refreshInterval: {{ .Values.externalSecrets.refreshInterval | default "1h" }}
  secretStoreRef:
    name: {{ .Values.externalSecrets.secretStoreRef.name }}
    kind: {{ .Values.externalSecrets.secretStoreRef.kind | default "ClusterSecretStore" }}
  target:
    name: dataplane-secrets-encryption
    creationPolicy: Owner
  data:
    {{- toYaml .Values.externalSecrets.secretsEncryption.data | nindent 4 }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: queue-redis-token
  annotations:
    external-secrets.io/force-sync: {{ now | date "2006-01-02T15:04:05Z" | quote }}
spec:
  refreshInterval: {{ .Values.externalSecrets.refreshInterval | default "1h" }}
  secretStoreRef:
    name: {{ .Values.externalSecrets.secretStoreRef.name }}
    kind: {{ .Values.externalSecrets.secretStoreRef.kind | default "ClusterSecretStore" }}
  target:
    name: queue-redis-token
    creationPolicy: Owner
  data:
    {{- toYaml .Values.externalSecrets.queueRedis.data | nindent 4 }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: kvstore-redis-token
  annotations:
    external-secrets.io/force-sync: {{ now | date "2006-01-02T15:04:05Z" | quote }}
spec:
  refreshInterval: {{ .Values.externalSecrets.refreshInterval | default "1h" }}
  secretStoreRef:
    name: {{ .Values.externalSecrets.secretStoreRef.name }}
    kind: {{ .Values.externalSecrets.secretStoreRef.kind | default "ClusterSecretStore" }}
  target:
    name: kvstore-redis-token
    creationPolicy: Owner
  data:
    {{- toYaml .Values.externalSecrets.kvstoreRedis.data | nindent 4 }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: agent-key
  annotations:
    external-secrets.io/force-sync: {{ now | date "2006-01-02T15:04:05Z" | quote }}
spec:
  refreshInterval: {{ .Values.externalSecrets.refreshInterval | default "1h" }}
  secretStoreRef:
    name: {{ .Values.externalSecrets.secretStoreRef.name }}
    kind: {{ .Values.externalSecrets.secretStoreRef.kind | default "ClusterSecretStore" }}
  target:
    name: agent-key
    creationPolicy: Owner
  data:
    {{- toYaml .Values.externalSecrets.agentKey.data | nindent 4 }}
{{- end }}
