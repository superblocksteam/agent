# Resolve @superblocksteam packages from public npmjs (sdk-api, types, etc.)
# GHPR auth kept for backwards compatibility with any private packages.
@superblocksteam:registry=https://registry.npmjs.org/

//npm.pkg.github.com/:_authToken=${NPM_TOKEN}
